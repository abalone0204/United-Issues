<nav class="customnavbar shadow" role="navigation">
  <!-- Brand and toggle get grouped for better mobile display -->
  <div class="navbar-header col-md-5">
    <%= link_to "United Issues", root_path, class:"navbar-brand brand" %>
  </div>
  <!-- Collect the nav links, forms, and other content for toggling -->
  <div class="collapse navbar-collapse">
    <ul class="nav navbar-nav">
      <li><%= link_to t('attributes.posts'), posts_path %></li>
    </ul>
    <ul class="nav navbar-nav navbar-right user-nav">
      <% unless user_signed_in? %>
        <li>
          <%= link_to new_user_registration_path do%>
          <span class="glyphicon glyphicon-user registration"></span>註冊
          <% end %>
        </li>
        <li>
          <%= link_to "登入", new_user_session_path %> 
        </li>
      <% end %>
      <% if user_signed_in? %>
        <% if current_user.admin? %>
          <li>
            <%= link_to admin_posts_path do%>
            <span class="icon-material-adb change-mode"></span> Admin
            <% end %>
          </li>
        <% end %>
      <li>
        <%= link_to current_user.name, '#' %>
      </li>
      <li>
        <%= link_to "登出", destroy_user_session_path, method: :delete %>
      </li>
      <% end %>
    </ul>
    </div><!-- /.navbar-collapse -->
  </nav>