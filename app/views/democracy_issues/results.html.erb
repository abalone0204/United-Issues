<h1>結果</h1>

<table class="table">
  <thead>
    <th>候選人</th>
    <th>得票數</th>
    <th>得票率</th>
  </thead>
  <tbody>
    <% @candidates.each do |candidate| %>
    <tr>
      <td><%= candidate.name %></td>
      <td><%= candidate.votes.count %></td>
      <td><%= vote_proportion(candidate) %></th>
    </tr>
    <% end %>
  </tbody>
</table>